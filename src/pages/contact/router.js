import React from 'react';
import { Route } from 'react-router';
import Contact from './index';

export default (
        <Route>
            <Route component={Contact} path={ Contact.path } />
        </Route>
        );
